# Release test matrix

Release version: `TBD`
Release date: `TBD`
Release Foundry model variant: `TBD`
Package lock commit: `TBD`
Release notes source: `TBD`
Model provenance source: `TBD`
Runtime framework notices source: `TBD`
Single-file experiment record: `TBD` (future information only; folder/ZIP deployment remains version 1)

The Foundry model must exactly match the configured application model. Each source header must use exact format `<file name> sha256:<lowercase SHA-256>`: use [the release-notes template](release-notes-template.md), [model-provenance template](model-provenance-template.json), and [runtime/framework notices template](runtime-framework-notices-template.txt).


Before running the publisher, replace the headers and both `<version>` placeholders. For the architecture being published, every row below must be exactly `Passed` with retained evidence in the final column. The three source headers are filename-and-hash bindings to the exact external release inputs; do not enter a repository template name or an un-hashed path.

| Area | x64 | ARM64 | Evidence / notes |
|---|---|---|---|
| Self-contained unpackaged folder launch on clean Windows 11 24H2 | Pending | Pending | |
| ZIP extracted from a Mark-of-the-Web download | Pending | Pending | Record SmartScreen wording; do not bypass policy in instructions. |
| No .NET SDK, Windows App SDK runtime, or Foundry CLI installed | Pending | Pending | |
| Exact CPU model variant resolves, downloads, and loads | Pending | Pending | |
| Known 16 kHz PCM transcription completes without pacing | Pending | Pending | |
| Cancel download, append, and response read; later session recovers | Pending | Pending | |
| First-run online model download | Pending | Pending | |
| Cached launch while offline | Pending | Pending | |
| Uncached launch while offline returns actionable error | Pending | Pending | |
| Synthetic flat corpus: 30 valid audio-only MP4s | Pending | Pending | |
| Synthetic nested corpus: same 30 mirrored VTTs | Pending | Pending | |
| Media fixture matrix: valid control plus malformed/no-audio/empty/unsupported cases | Pending | Pending | Retain `WinBulkTranscript.MediaIntegrationProbe` JSON report and binary provenance/SHA-256. |
| Media extraction cancellation and temporary-WAV cleanup | Pending | Pending | Retain a cancellation probe report; final-VTT cleanup remains a batch/workflow test. |
| End-to-end media/VAD/Foundry/coordinator/VTT success, cancellation, and failure preservation | Pending | Pending | Retain `WinBulkTranscript.WorkflowIntegrationProbe` JSON reports for each scenario. |
| Long input, low disk, read-only output, Unicode path | Pending | Pending | |
| Cancellation, sleep/resume, and window-close coordination | Pending | Pending | |
| High contrast, 100–200% scale, keyboard-only, long strings | Pending | Pending | |

## Release communication and future single-file experiment

Before a release is marked supported, retain completed release notes based on [the template](release-notes-template.md). The notes must name the version and architecture artifact and include the unsigned SmartScreen warning; do not substitute a generic repository document for release-specific notes.

Record the single-file deployment experiment here or in a linked immutable evidence file. Include the commands, architecture, host details, Foundry native-probing result after extraction, and any cleanup/recovery observation. This experiment is future information only: it never changes the version 1 folder/ZIP artifact decision or compensates for a failed clean-machine folder test.

| Future-information check | x64 | ARM64 | Evidence / notes |
|---|---|---|---|
| Literal single-file publish and Foundry native probing after extraction | Pending | Pending | Retain command, artifact version, host, outcome, and diagnostic evidence. |

## Artifact checksums

The ZIP checksum is written only after the archive is complete, so its adjacent sidecar and release record are the canonical receipt rather than a self-referential value inside the ZIP.

| Artifact | SHA-256 | Generated by |
|---|---|---|
| `WinBulkTranscript-<version>-win-x64.zip` | Publisher writes the matching `.zip.sha256` sidecar and `.release-record.json`. | `scripts/Publish-Release.ps1` |
| `WinBulkTranscript-<version>-win-arm64.zip` | Publisher writes the matching `.zip.sha256` sidecar and `.release-record.json`. | `scripts/Publish-Release.ps1` |
